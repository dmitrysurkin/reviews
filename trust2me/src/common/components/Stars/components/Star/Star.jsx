import React from 'react';
import { StarFilled } from "@ant-design/icons";


const Star = ({ classes }) => {
    return (
        <StarFilled className={classes.star} />
    );
};

export default Star;